import { Plugin } from 'vite';
import { ObfuscatorOptions } from 'javascript-obfuscator';

interface IOptions {
    includeRegEx: RegExp;
    excludeRegEx: RegExp;
    options: ObfuscatorOptions;
}

declare function plugin(opts?: IOptions): Plugin;

export { plugin as default };
